[build-system]
requires = ["setuptools >= 61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ed_colony_shopper"
version = "0.0.0"

[tool.setuptools.packages.find]
where = ["src"]
include = ["ed_colony_shopper*"]

[tool.setuptools]
script-files = ["src/ed-colony-shopper.py"]
